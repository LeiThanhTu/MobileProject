name: test
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  confetti: ^0.8.0
  intl: ^0.20.2
  sqflite: ^2.3.2
  path: ^1.9.1
  path_provider: ^2.1.2
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  flutter_svg: ^2.0.9
  google_fonts: ^6.1.0
  http: ^1.2.0
  flutter_local_notifications: ^19.1.0
  timezone: ^0.10.0
  encrypt: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/quiz-logo.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true
  assets:
    - assets/db.db
    - assets/images/
